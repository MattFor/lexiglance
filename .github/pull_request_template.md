## What and why

<!-- What the change does and the problem it solves. Link the issue if there is one. -->

## Checklist

- [ ] `cmake --build --preset debug` builds without warnings
- [ ] `ctest --preset debug` passes, with tests for new behaviour
- [ ] `clang-format` applied and `clang-tidy` reports nothing new
- [ ] Anti-cheat rules kept (no input injection, no process access, see `docs/anticheat.md`)
- [ ] User-visible changes noted in `CHANGELOG.md`
