# Leaks inside system libraries, which LeakSanitizer reports but Lexiglance cannot free.
# fontconfig keeps its configuration for the life of the process once Pango initialises it.
leak:libfontconfig.so
